## Цель
Заменить «некрасивый плавающий круг» в шапке (SVG `LimeMark` внутри стеклянного бейджа) на картинку маскота-лайма.

## Что меняем
Только `src/components/app/Header.tsx`, левый плавающий бейдж лого:

- Сейчас:
  ```tsx
  <div className="animate-float relative grid h-11 w-11 ... rounded-2xl glass glow-lime">
    <LimeMark className="h-7 w-7" />
  </div>
  ```
- Заменяем содержимое на изображение маскота (`BRAND.mascot.image`, тот же файл, что загрузил пользователь).
  - Используем `<img>` (или существующий `LimoAvatar`) размером ~`h-11 w-11`, `object-contain`.
  - Анимацию `animate-float` оставляем.
  - Убираем стеклянный круг/обводку/`glow-lime`, чтобы мультяшный лайм (у него своя обводка и тень) смотрелся чисто; оставляем мягкий drop-shadow для свечения.
  - Прописываем осмысленный `alt` (имя маскота).

## Что НЕ трогаем
- Ассеты не создаём — `limo-mascot.png` уже в проекте.
- `LimeMark` остаётся в `Mascot.tsx` (может использоваться в других местах).
- Бэкенд/контракт, остальная вёрстка шапки.

## Проверка
- preview: в шапке вместо круга — маскот-лайм, плавно «дышит», без лишнего стеклянного круга, ничего не обрезано.